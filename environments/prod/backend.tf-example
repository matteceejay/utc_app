terraform {
  backend "s3" {
    bucket       = "<TFSTATE_BUCKET_NAME>"
    key          = "prod/terraform.tfstate"
    region       = "<AWS_REGION>"
    encrypt      = true
    use_lockfile = true
  }
}